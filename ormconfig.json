{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "anders",
  "password": "postgres",
  "database": "coffee",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "autoLoadEntities": true,
  "migrations": ["migration/*.js"],
  "cli": {
    "migrationsDir": "migration"
  }
}
